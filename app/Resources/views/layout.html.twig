{% extends '::base.html.twig' %}

{% block body %}
    {% block sidebar %}
        {% set currentPath = path(app.request.attributes.get('_route'), app.request.attributes.get('_route_params')) %}
        <nav class="navbar navbar-inverse navbar-fixed-top">
            <div class="container">
                <div class="navbar-header">
                    <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#navbar" aria-expanded="false" aria-controls="navbar">
                        <span class="sr-only">Toggle Navigation</span>
                        <span class="icon-bar"></span>
                        <span class="icon-bar"></span>
                        <span class="icon-bar"></span>
                    </button>
                    <a class="navbar-brand" href="{{ path('homepage') }}">Library</a>
                </div>
                <div id="navbar" class="navbar-collapse collapse">
                    <ul class="nav navbar-nav">
                        <li {{ currentPath == path('list_media') ? 'class="active"' : ''}}><a href="{{ path('list_media') }}">Media</a></li>
                        {#<li {{ currentPath == path('article') ? 'class="active"' : ''}}><a href="{{ path('article') }}">Read</a></li>#}
                        {#<li {{ currentPath == path('roster') ? 'class="active"' : ''}}><a href="{{ path('roster') }}">The Team</a></li>#}
                        {#<li {{ currentPath == path('contact') ? 'class="active"' : ''}}><a href="{{ path('contact') }}">Contact</a></li>#}
                    </ul>
                    <ul class="nav navbar-nav pull-right">
                        <li class="dropdown">
                            <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="true">Gestion <span class="caret"></span></a>
                            <ul class="dropdown-menu">
                                <li {{ currentPath == path('index_type') ? 'class="active"' : ''}}><a href="{{ path('index_type') }}">Type</a></li>
                                <li {{ currentPath == path('index_genre') ? 'class="active"' : ''}}><a href="{{ path('index_genre') }}">Genre</a></li>
                            </ul>
                        </li>
                        <li><a href="{{ path('fos_user_security_logout') }}">Logout</a></li>
                    </ul>
                </div>
            </div>
        </nav>
    {% endblock %}
    <div class="container">
        {% block head_script %}{% endblock %}
        {% block content %}{% endblock %}
    </div>

{% endblock %}